import React from "react";
import { DarkModeSwitch } from "react-toggle-dark-mode";

export const ThemeToggler = ({ mode }) => {
  return <DarkModeSwitch onChange={()=>{}} checked={mode} size={30} />;
};
